using UnityEngine;

namespace UMol {
    public static class MSMSWrapper {
        public static void createMSMSSurface(int idFrame, UnityMolSelection sel, ref MeshData meshd,
            float density = 15.0f, float probeRad = 1.4f, string tempPath = "") {
            meshd = null;
        }
    }
}
